export enum TileType {
    Floor = 1,         // Walkable tile
    Wall = 2,          // Collidable tile, like a wall
    PlayerSpawn = 3,   // Where the player can spawn
    Crate = 4,         // Represents a crate
    PickupSpawn = 5    // Represents a spawn point for pickups
  }
  